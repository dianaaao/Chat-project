const socket = io();
// const currentUserId = {{ current_user.id }};
// const currentGroupId = {{ group.id }};

document.getElementById("my-chat-item").addEventListener("click", () => {
    // open chat include part 
    

});